##Copyright 2005 Harvard University / Howard Hughes Medical Institute (HHMI) All Rights Reserved
$page.setTitle("Add Subject")
$page.setLinkColor($ui.alink)
$page.setVlinkColor($ui.vlink)
#set ( $subjectHeadings = ["ID","Labels", "Date" ] )
#set($months = ["January", "February", "March", "April", "May", "June", "July", "August", "September", "October", "November", "December"])
#set($days = [ 1..31 ])
#set($years = [ $!turbineUtils.getYear()..1900])
#if ($turbineUtils.GetPassedParameter("popup",$data))
	#set ($popup = $turbineUtils.GetPassedParameter("popup",$data) )
#else
	#set ($popup = "false")
#end

<form id="editSubjectForm" method="post" action="$link.setAction("EditSubjectAction")" onsubmit="return validateEditForm();">
#parse("/screens/xnat_subjectData/xnat_subjectData_edit.vm")
</form>
			
